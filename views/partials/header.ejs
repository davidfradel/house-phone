<div id="navbar-top">
  <nav class="navbar navbar-expand-md navbar-light bg-white fixed">
      <div class="container">
          <a class="navbar-brand nav-external" id="logo" href="/">Renard Bleu</a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#nav" aria-controls="nav" aria-expanded="false"
              aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="nav">
              <ul class="navbar-nav ml-auto">
                  <li class="nav-item active">
                      <a class="nav-link" href="/#home">Accueil</a>
                  </li>
                  <li class="nav-item">
                    <% if(!isConnected){ %>
                      <a class="nav-link" href="/#services">Services</a>
                    <% } else{ %>  
                        <% } %>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="/#work">Fonctionnement</a>
                  </li>
                  <li class="nav-item">
                    <% if(!isConnected){ %>
                        <a class="nav-link" href="/#clients">Clients</a>
                      <% } else{ %>  
                          <% } %>
                  </li>
                  <li class="nav-item">
                    <% if(!isConnected){ %>
                        <a class="nav-link" href="/#about">A propos</a>
                      <% } else{ %>  
                        <a class="nav-link" id="prediction" href="/prediction">Prédire</a>
                    <% } %>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" href="/#contact">Contact</a>
                  </li>
                  <li class="nav-item">
                    <% if(isConnected){ %>
                        <a class="nav-link" id="connection"  href="/logout">Déconnexion</a>
                      <% } else{ %>  
                        <a class="nav-link" id="connection"  href="/login">Connexion</a>
                     <% } %>
                  </li>
              </ul>
          </div>
      </div>
  </nav> 
</div>